# Copy to  deploy/.env  on the dev/test server (do NOT commit the real file).
# Used only by docker-compose to set the MySQL container's root password.
# This value must match DB_PASSWORD in backend/.env.development and .env.testing.
MYSQL_ROOT_PASSWORD=change-me-strong-root-password
