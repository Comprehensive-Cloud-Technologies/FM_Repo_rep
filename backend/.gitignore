
# Elastic Beanstalk Files
.elasticbeanstalk/*
!.elasticbeanstalk/*.cfg.yml
!.elasticbeanstalk/*.global.yml

# User uploaded files - NEVER delete these during deployment
uploads/query-images/
uploads/

# Firebase service account key (contains secrets — never commit)
*firebase-adminsdk*.json
firebase-service-account.json
